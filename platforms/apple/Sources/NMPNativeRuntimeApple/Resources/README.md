# Generated resources

`TrustedShell` is a packaged snapshot of the canonical source in
`web/trusted-shell`. Refresh it with `platforms/apple/scripts/sync-trusted-shell`.
The trusted-shell contract test compares the packaged snapshot byte-for-byte.
