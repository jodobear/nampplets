import SwiftUI
import RuntimeWorkbenchFeature

@main
struct RuntimeWorkbenchApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 1180, height: 780)
    }
}
